package com.kiran.modifier;

public class MemberModifier {
// private member ->  only with the current class
// default member - only with current package
// protected member -> current package + outside the package in child class
// public members -> can be access from outside package

// Private<default<protected<public

// final variable
// final instance variable
// must initialize -> declaration time, inside instance block, inside constructor

// final static variables
// must initialize -> declaration time, inside static block

// final local variable
// for temporary requirement variable ->  create, no default value by jvm

}
